<MudIconButton Icon="fa-solid fa-floppy-disk" Variant="Variant.Filled" Color="Color.Success" OnClick="(e => OnClick.InvokeAsync())" Disabled="Disable"/>

@code {
    [Parameter]
    public EventCallback<bool> OnClick { get; set; }

    [Parameter]    
    public bool Disable { get; set; }
}
